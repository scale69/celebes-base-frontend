type PageProps = {
    params: { slug: string };
    searchParams: Record<string, string | string[] | undefined>;
};
export default function NewsHeaderTemplate({ params, searchParams }: PageProps) {
    return (
        <div className=''>

        </div>
    )
}